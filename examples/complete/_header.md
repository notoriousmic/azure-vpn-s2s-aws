# Complete Example — Greenfield S2S VPN with All Options

Creates a new AWS VPC + VPG with all optional variables explicitly set:

- Custom naming prefix
- Custom BGP ASNs for both Azure and AWS
- Custom VPN Gateway SKU and generation
- Custom CIDR ranges for both Azure VNet and AWS VPC
- Custom availability zones
- All tags specified
# Complete Example — S2S VPN with All Options

Deploys Pattern 1 (Site-to-Site VPN) with all optional variables explicitly set:

- Custom naming prefix
- Custom BGP ASN values
- Custom VPN Gateway SKU
- Custom CIDR ranges for both Azure and AWS
- All outputs demonstrated
